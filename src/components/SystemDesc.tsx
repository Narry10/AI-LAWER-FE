import React from 'react';
import { Typography } from 'antd';

const SystemDesc: React.FC = () => (
  <Typography.Text style={{ fontSize: 14, color: '#888', marginLeft: 8 }}>
    Hệ thống quản lý site CMS Sites
  </Typography.Text>
);

export default SystemDesc;
